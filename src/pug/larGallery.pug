doctype html
html(lang='en')

  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge')
    meta(name='viewport', content='width=device-width, initial-scale=1, shrink-to-fit=no')
    meta(name='description', content='')
    meta(name='author', content='')
    link(rel="stylesheet" href="css/larGallery/larGallery.css")

    title SB Admin - Start Bootstrap Template

    include includes/css/core.pug

    include includes/css/custom.pug

  body#page-top.fixed-nav.sticky-footer.bg-dark.main
    include includes/navbar.pug
    .content-wrapper
      .container-fluid
        // Breadcrumbs
        ol.breadcrumb
          li.breadcrumb-item
            a(href='#') Lar-ui
          li.breadcrumb-item.active 轮播图组件
        div.result
          h3
            a.headerLink |轮播图组件
          p
            code larGallery
            |实现图片轮播的方法
          h3
            a.headerLink |使用方法
          p
            div.code-html html，只需要一个容器即可，div或section都可以。class最好是写成css-gallery
              - var divContainerH = '<div id="gallery" class="css-gallery"></div>';
              code.block #{divContainerH}
            p
            div.code-js js，items是需要轮播图片的数据
              code.block $('#gallery1').larGallery({
              code.block &nbsp;&nbsp;errorImage : 'larGalleryWall/1.jpg',
              code.block &nbsp;&nbsp;showNum: 3,
              code.block &nbsp;&nbsp;items: [{href:'https://www.baidu.com',src:'larGalleryWall/1.jpg'},
              code.block &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;{href:'https://www.baidu.com',src:'larGalleryWall/2.jpg'},
              code.block &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;{href:'https://www.baidu.com',src:'larGalleryWall/3.jpg'},
              code.block &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;{href:'https://www.baidu.com',src:'larGalleryWall/4.jpg'}]
              code.block });
            p
            div.code-css css
              code.block .css-gallery {
              code.block &nbsp;&nbsp;width: 1000px;
                span.annotation &nbsp;&nbsp;&nbsp;&nbsp;//该宽度可自由调节
              code.block &nbsp;&nbsp;position: relative;
              code.block &nbsp;&nbsp;margin: 0 auto;
              code.block &nbsp;&nbsp;padding: 0;
              code.block }
              code.block .css-gallery-list-wrap {
              code.block &nbsp;&nbsp;overflow: hidden;
              code.block &nbsp;&nbsp;white-space: nowrap;
              code.block &nbsp;&nbsp;position: relative;
              code.block &nbsp;&nbsp;margin: 0;
              code.block &nbsp;&nbsp;padding: 0;
              code.block }
              code.block .css-gallery-list {
              code.block &nbsp;&nbsp;position: relative;
              code.block &nbsp;&nbsp;padding: 0;
              code.block }
              code.block .css-gallery-item {
              code.block &nbsp;&nbsp;display: inline-block;
              code.block &nbsp;&nbsp;padding: 0 16px;
                span.annotation &nbsp;&nbsp;&nbsp;&nbsp;//可以按需自由调整
              code.block }
              code.block .css-gallery-item img {
              code.block &nbsp;&nbsp;width: 100%;
              code.block &nbsp;&nbsp;height: 300px;
                span.annotation &nbsp;&nbsp;&nbsp;&nbsp;//可以按需自由调整
              code.block &nbsp;&nbsp;vertical-align: middle;
              code.block }
              code.block .css-gallery-btn.prev {
              code.block &nbsp;&nbsp;background-image: url(../../larGallery/left-arrow.png);
                span.annotation &nbsp;&nbsp;&nbsp;&nbsp;//该图标可自行更改
              code.block &nbsp;&nbsp;margin-top: -36px;
                span.annotation &nbsp;&nbsp;&nbsp;&nbsp;//可自由调整
              code.block &nbsp;&nbsp;position: absolute;
              code.block &nbsp;&nbsp;top: 120px;
                span.annotation &nbsp;&nbsp;&nbsp;&nbsp;//可自由调整
              code.block &nbsp;&nbsp;left: -40px;
                span.annotation &nbsp;&nbsp;&nbsp;&nbsp;//可自由调整
              code.block &nbsp;&nbsp;width: 40px;
                span.annotation &nbsp;&nbsp;&nbsp;&nbsp;//宽高可调整，但必须给值
              code.block &nbsp;&nbsp;height: 40px;
              code.block }
              code.block .css-gallery-btn.next {
              code.block &nbsp;&nbsp;background-image: url(../../larGallery/right-arrow.png);
                span.annotation &nbsp;&nbsp;&nbsp;&nbsp;//该图标可自行更改
              code.block &nbsp;&nbsp;margin-top: -36px;
                span.annotation &nbsp;&nbsp;&nbsp;&nbsp;//可自由调整
              code.block &nbsp;&nbsp;position: absolute;
              code.block &nbsp;&nbsp;top: 120px;
                span.annotation &nbsp;&nbsp;&nbsp;&nbsp;//可自由调整
              code.block &nbsp;&nbsp;right: -40px;
                span.annotation &nbsp;&nbsp;&nbsp;&nbsp;//可自由调整
              code.block &nbsp;&nbsp;width: 40px;
                span.annotation &nbsp;&nbsp;&nbsp;&nbsp;//宽高可调整，但必须给值
              code.block &nbsp;&nbsp;height: 40px;
              code.block }
          h3
            a.headerLink |展示效果
          div.container
            div#gallery1
          br
          p
          h3
            a.headerLink |属性说明
          -
           var properties = [{ title:'errorImage',type:'string',default:'../../lar-ui/imgs/default.png',description:'图片加载错误时显示的图片的路径'},
                             {title:'showNum',type:'int',default:3,description:'当前视图中显示的图片数量'},
                             {title:'items',type:'array',default:'undefined',description:'轮播图中图片的数据。数组中每个对象的属性分别为：href：string，当前图片跳转的路径;src：string，图片路径;extras;object，其中$dom是与当前图片相关的描述的DOM节点的jquery对象，position(string)指的是描述内容相对于图片的显示位置，默认为after图片之后，如果设置为其他值则处理为prepend图片之前'},
                             {title:'speed',type:'int',default:3000,description:'自动轮播的速度'},
                             {title:'direction',type:'string',default:'left',description:'轮播的方向，left为向左滑动，right为向右滑动，stop为停止滑动'}];
          each property in properties
            div.description
              li.description-title #{property.title}
              div.description-type
                span 类型：
                span.value #{property.type}
              div.description-default
                span 默认值：
                span.value #{property.default}
              div.description-detail
                span 描述：
                span.value #{property.description}

      include includes/footer.pug

      include includes/scroll-to-top.pug

      include includes/js/core.pug

      include includes/js/custom.pug

      script(src='../artnchina/lib/jquery-1.11.2.min.js')
      script(src='larGallery/larGallery.js')
      script.
        $('#gallery1').larGallery({
          errorImage : 'larGalleryWall/1.jpg',
          showNum: 3,
          items: [{href:'https://www.baidu.com',src:'larGalleryWall/1.jpg'},
                  {href:'https://www.baidu.com',src:'larGalleryWall/2.jpg'},
                  {href:'https://www.baidu.com',src:'larGalleryWall/3.jpg'},
                  {href:'https://www.baidu.com',src:'larGalleryWall/4.jpg'}]
        });
      // Toggle between fixed and static navbar
      script.
        $('#toggleNavPosition').click(function() {
        $('body').toggleClass('fixed-nav');
        $('nav').toggleClass('fixed-top static-top');
        });

      // Toggle between dark and light navbar
      script.
        $('#toggleNavColor').click(function() {
        $('nav').toggleClass('navbar-dark navbar-light');
        $('nav').toggleClass('bg-dark bg-light');
        $('body').toggleClass('bg-dark bg-light');
        });
